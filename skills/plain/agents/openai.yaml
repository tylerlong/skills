interface:
  display_name: "Plain"
  short_description: "Write concrete, structured Codex chat"
  default_prompt: "Use $plain to make this Codex response concrete, claim-first, and easy to scan."
policy:
  allow_implicit_invocation: false
