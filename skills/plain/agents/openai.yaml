interface:
  display_name: "Plain"
  short_description: "Format the final Codex response clearly"
  default_prompt: "Use $plain to format this final response with clear structure and existing evidence."
policy:
  allow_implicit_invocation: false
