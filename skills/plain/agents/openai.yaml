interface:
  display_name: "Plain"
  short_description: "Format the final Codex response clearly"
  default_prompt: "Use $plain to format the final response."
policy:
  allow_implicit_invocation: false
