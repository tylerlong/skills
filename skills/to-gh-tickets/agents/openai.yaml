interface:
  display_name: "To GitHub Tickets"
  short_description: "Publish tickets with verified GitHub relationships"
  default_prompt: "Use $to-gh-tickets to turn the current approved specification into verified GitHub-native child tickets."
policy:
  allow_implicit_invocation: false
