interface:
  display_name: "To GitHub Tickets"
  short_description: "Postprocess GitHub-native ticket relationships"
  default_prompt: "Use $to-gh-tickets to run to-tickets, then ensure native GitHub relationships and remove duplicate relationship sections from its published Child Tickets."
policy:
  allow_implicit_invocation: false
