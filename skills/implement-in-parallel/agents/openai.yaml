interface:
  display_name: "Implement in Parallel"
  short_description: "Deliver isolated Batch PRs through exact green main CI"
  default_prompt: "Use $implement-in-parallel with a Parent Ticket to implement its ready Child Tickets in an isolated Batch Run and deliver them through exact green main CI."
policy:
  allow_implicit_invocation: false
