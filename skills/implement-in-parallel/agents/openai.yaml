interface:
  display_name: "Implement in Parallel"
  short_description: "Implement child tickets concurrently"
  default_prompt: "Use $implement-in-parallel to implement the ready direct Child Tickets of this GitHub Parent Ticket with concurrent Worker Agents."
policy:
  allow_implicit_invocation: false
