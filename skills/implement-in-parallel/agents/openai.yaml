interface:
  display_name: "Implement in Parallel"
  short_description: "Recover and serialize isolated child-ticket deliveries"
  default_prompt: "Use $implement-in-parallel with a Parent Ticket to start or resume its isolated Batch Run, deliver all progressable Child Tickets, and recover any owned failed delivery before ordinary work continues."
policy:
  allow_implicit_invocation: false
