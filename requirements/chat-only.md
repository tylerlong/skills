# chat-only requirements

1. While chat-only mode is on, the agent must not change the current code base, except that the prohibition is lifted for a single user request when the user asks the agent to change the current code base and is automatically restored once that request is handled. The mode turns on or off only on an explicit user request.
